<%- include('../partials/header', { title: 'Mostrar Departaments' }) %>

    <table class="table">
        <tr>
            <th>Nombre</th>
            <th>Ubicacio</th>
        </tr>
        <% departamentos.forEach(departamento => { %>
            <tr>
                <td><%= departamento.nombre %></td>
                <td><%= departamento.ubicacio %></td>
            </tr>
        <% }); %>
    </table>

    <%- include('../partials/footer') %>